# Tablet JSON inbox

Put exported tablet JSON files in this folder.

BridgeDesk reads every `*.json` file here when it starts. If this folder is
empty, the app falls back to `data/tablet_submissions.json` so the sample
workflow still opens.
